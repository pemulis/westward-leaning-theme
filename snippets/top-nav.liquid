	<div id='header-ribbon' class='hidden-xs'><a href='/'>{{ shop.name }}</a></div>
    <nav class='navbar navbar-default' role='navigation'>
		<div class='navbar-header'>
        	<button type='button' class='navbar-toggle' data-toggle='collapse' data-target='#top-menu'>
          		<span class='sr-only'>Toggle navigation</span>
          		<span class='icon-bar'></span>
          		<span class='icon-bar'></span>
          		<span class='icon-bar'></span>
        	</button>
          	<a class='navbar-brand visible-xs' href='/'>{{ shop.name }}</a>
		</div>
		<div id='top-menu' class='collapse navbar-collapse'>
        	<ul class='nav navbar-nav'>
				{% for link in linklists.main-menu.links %}
                <li {% if link.title contains 'ABOUT' %}class='dropdown'{% elsif link.title contains 'AS SEEN ON' %}class='dropdown'{% endif %}>
                  <a href='{{ link.url }}' {% if link.title contains 'ABOUT' %}data-toggle='dropdown' class='dropdown-toggle'{% elsif link.title contains 'AS SEEN ON' %}data-toggle='dropdown' class='dropdown-toggle'{% elsif link.title contains 'HOLIDAY' %}class='holiday visible-xs'{% endif %}>{{ link.title }}</a>
                  {% if link.title contains 'ABOUT' %}
                    <ul class='dropdown-menu'>
                      {% for dropdownlink in linklists.about.links %}
                        <li><a href='{{dropdownlink.url}}'>{{dropdownlink.title|upcase}}</a></li>
                      {% endfor %}
                    </ul>
                  {% elsif link.title contains 'AS SEEN ON' %}
                    <ul class='dropdown-menu'>
                      {% for dropdownlink in linklists.lookbook.links %}
                        <li><a href='{{dropdownlink.url}}'>{{dropdownlink.title|upcase}}</a></li>
                      {% endfor %}
                    </ul>
                  {% endif %}
                </li>
                {% endfor %}
          		<li class='cart-link hidden-md hidden-sm hidden-xs'><a href='/cart'>BAG ({{ cart.item_count }})</a></li>
                <li class='visible-xs'><a href='/pages/help/'>CUSTOMER SUPPORT</a></li>
                <li class='visible-md visible-sm visible-xs'><a href='/cart'>BAG ({{ cart.item_count }})</a></li>
        	</ul>
      	</div>
    </nav>


    <div id='sticky-mini-menu' class='hidden hidden-xs'>
      <nav class='navbar navbar-default' role='navigation'>
        <ul class='nav navbar-nav'>
				{% for link in linklists.main-menu.links %}
                <li {% if link.title contains 'ABOUT' %}class='dropdown'{% elsif link.title contains 'AS SEEN ON' %}class='dropdown'{% endif %}>
                  <a href='{{ link.url }}' {% if link.title contains 'ABOUT' %}data-toggle='dropdown' class='dropdown-toggle'{% elsif link.title contains 'AS SEEN ON' %}data-toggle='dropdown' class='dropdown-toggle'{% elsif link.title contains 'HOLIDAY' %}class='holiday visible-xs'{% endif %}>{{ link.title }}</a>
                  {% if link.title contains 'ABOUT' %}
                    <ul class='dropdown-menu'>
                      {% for dropdownlink in linklists.about.links %}
                        <li><a href='{{dropdownlink.url}}'>{{dropdownlink.title|upcase}}</a></li>
                      {% endfor %}
                    </ul>
                  {% elsif link.title contains 'AS SEEN ON' %}
                    <ul class='dropdown-menu'>
                      {% for dropdownlink in linklists.lookbook.links %}
                        <li><a href='{{dropdownlink.url}}'>{{dropdownlink.title|upcase}}</a></li>
                      {% endfor %}
                    </ul>
                  {% endif %}
                </li>
                {% endfor %}
          		<li class='cart-link hidden-md hidden-sm hidden-xs'><a href='/cart'>BAG ({{ cart.item_count }})</a></li>
                <li class='visible-md visible-sm visible-xs'><a href='/cart'>BAG ({{ cart.item_count }})</a></li>
        </ul>
      </nav>
    </div>